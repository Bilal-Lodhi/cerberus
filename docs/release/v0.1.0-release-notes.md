<!--
  RELEASE NOTES — Cerberus v0.1.0

  Prepared against https://github.com/Bilal-Lodhi/cerberus. All repository links
  below use the real owner. This file is the body of the GitHub Release for tag
  v0.1.0.
-->

# Cerberus v0.1.0 — First Independent Open-Source Release

Cerberus 0.1.0 is the first release of Cerberus as an independent open-source
project. Cerberus began as "Cerberus FinSec" in the Google Cloud Rapid Agent
Hackathon 2026 (Financial Services track, MongoDB partner track), where it was
built on Google Cloud Agent Builder with Gemini as the model. The AI boundary
was migrated to the OpenAI SDK later, during OpenAI Build Week 2026 (Agentic
Coding track). The project was subsequently extracted from the historical
`Google-Cloud-Hackathon` repository into this standalone repository. This
release is the state of that extraction: a self-hosted telemetry service that
ingests fine-grained terminal activity from a browser-based operator console,
scores it with a language model for insider-threat and data-exfiltration
indicators, and persists sessions, events and risk assessments in MongoDB. It is
an experimental research system, not a compliance product, and it is not
affiliated with, endorsed by, or sponsored by Google, OpenAI or MongoDB.

## Highlights

- **API-key authentication** on the HTTP API, compared in constant time. The
  middleware accepts `Authorization: Bearer <key>` and `X-API-Key: <key>`,
  admits `GET /health` without a credential, and returns an identical
  `401 UNAUTHENTICATED` response for a missing and a mismatched key.
- **Explicit CORS allow-list.** Cross-origin access is granted only to origins
  named in `CERBERUS_CORS_ORIGINS`. The previous `origin: "*"` wildcard is gone;
  development-only localhost defaults apply only when `CERBERUS_DEV_MODE=true`.
- **Fail-closed configuration validation.** The API refuses to start when
  `OPENAI_API_KEY`, `CERBERUS_API_KEY` or `CERBERUS_MCP_TOKEN` is missing, and
  refuses to start with `CERBERUS_DEV_MODE=true` while `NODE_ENV=production`.
- **Micro-event telemetry ingestion** over `POST /api/v1/guardian/ingest`,
  covering twelve event types: keystrokes, paste triggers, code deltas, tab
  switches, window blur, copy attempts, dev-tools open, fullscreen exit,
  external app switch, submissions, edits and pastes.
- **Multi-layer deduplication** before inference is spent: identical
  risk-assessment id, code-hash equality to skip re-analysis of an unchanged
  workspace, a 128-entry micro-event fingerprint ring, and a behavioural-counter
  score blend.
- **AI risk analysis** producing a `RiskAssessmentPayload` with six risk
  dimensions, risk flags, an exfiltration report and behavioural anomalies.
- **Agentic auto-lock** at risk >= 75 and auto-clear below 25, propagated to
  in-memory session state and to MongoDB.
- **Threat scenario authoring** over `POST /api/v1/scenarios`, guarded by a
  deterministic regex pre-filter and a fail-closed AI classifier.
- **Session review** with a reconstructed event timeline and risk summary, plus
  session terminate (preserves data) and delete (removes data).
- **Natural-language auditor** over session records, restricted to a whitelisted
  subset of aggregation stages interpreted in-process.
- **MongoDB persistence through an MCP server**, with idempotent index creation
  and both a stdio transport and an HTTP adapter.
- **Automated test suite** using the Node.js built-in test runner (`node:test`)
  executed through `tsx`, wired up as `npm test`.
- **GitHub Actions continuous integration** covering the TypeScript workspaces,
  the Flutter console, the Docker image build, a secret scan and an advisory
  dependency audit.
- **Open-source documentation set**: `README.md`, `CONTRIBUTING.md`,
  `CODE_OF_CONDUCT.md`, `SECURITY.md`, `SUPPORT.md`, `CHANGELOG.md`, `NOTICE`,
  `docs/`, and the GitHub issue and pull request templates.
- **Docker Compose local startup** for the MongoDB dependency and the Cerberus
  services, and a multi-stage `Dockerfile` that runs both Node services in one
  non-root container.

## Breaking changes

This is the first release, so there is nothing to upgrade *from* within this
project's own release history. There is, however, **no upgrade path from the
hackathon-era code or data.** The historical names are not accepted as aliases,
fallbacks or compatibility shims: there is no dual-write path, no read-through of
legacy documents, no route alias and no feature flag that restores the old
behaviour. Data and client code that use the historical names must be renamed
before Cerberus 0.1.0 can read it. The full mapping, plus the rename sequence and
its risks, is in [`docs/migration.md`](../migration.md).

### Renamed MongoDB collections

| Historical | Current | Notes |
| --- | --- | --- |
| `test_suites` | `threat_scenarios` | Authored scenario matrices. |
| `assessment_sessions` | `monitored_sessions` | Session documents and aggregate counters. |
| `micro_events` | `micro_events` | Unchanged; the name was already domain-neutral. |
| `suspicion_reports` | `risk_assessments` | Risk assessment payloads. |

### Renamed database

| Historical | Current |
| --- | --- |
| `gorilla_agents` | `cerberus` |

The database name is the constant `DEFAULT_DATABASE_NAME` in
`packages/mcp-mongodb/src/tool-names.ts` and is overridable with
`MONGODB_DATABASE`. Renaming a collection does not rename a database.

### Renamed MCP tools

| Historical | Current | Notes |
| --- | --- | --- |
| `store_test_suite` | `store_threat_scenario` | |
| `get_test_suite` | `get_threat_scenario` | Argument `matrixId` unchanged. |
| `update_session_code` | `update_session_terminal_content` | Argument `submittedCode` → `terminalContent`. |
| `store_suspicion_report` | `store_risk_assessment` | Argument `report` unchanged. |
| `get_candidate_report` | `get_employee_risk_history` | Argument `candidateId` → `employeeId`. |
| `create_session`, `delete_session`, `append_micro_event`, `ingest_micro_events`, `update_session_counts`, `set_session_status`, `get_session_review`, `list_sessions`, `health_check` | unchanged | |

The authoritative list is `MCP_TOOL_NAMES` in
`packages/mcp-mongodb/src/tool-names.ts`. An unknown tool name is not merely
undocumented: the HTTP adapter answers `404 Unknown tool: <name>`, and the tool
registry is typed so that a name without a handler fails the TypeScript build.

### Renamed API routes and request fields

| Historical | Current |
| --- | --- |
| `POST /api/v1/generate` | `POST /api/v1/scenarios` |
| `POST /api/v1/generate/cancel` | `POST /api/v1/scenarios/cancel` |
| `GET /api/v1/guardian/stream/:id` | no equivalent; poll `GET /api/v1/guardian/sessions/:sessionId` |
| `problemCount` | `vectorCount` (integer, 1–25) |
| `difficultyMix` (`beginner`/`intermediate`/`advanced`) | `severityMix` (`low`/`medium`/`high`/`critical`) |
| `candidateId` | `employeeId` |
| `assessmentId` | `auditId` |
| `problemId` | `vectorId` |
| `submittedCode` | `terminalContent` |
| `suspicionPayload` / `suspicionReports` | `riskPayload` / `riskAssessments` |
| `plagiarismReport` | `exfiltrationReport` |
| `GeneratedTestSuite` / `GeneratedComplianceMatrix` | `ThreatScenarioMatrix` |
| `suiteId` | `matrixId` |

The current route table is documented in the header comment of
`apps/api/src/index.ts` and described in
[`docs/architecture.md`](../architecture.md).

### Removed Assessment-era type aliases

The Assessment-era type-alias layer that duplicated the domain types has been
removed, together with the legacy `GeminiClient` class name (replaced by
`OpenAIProvider`), hackathon-only files, tracked build artifacts and unrelated
cloud-project identifiers. Code that imported those aliases or that class must
import the domain types from `apps/api/src/types.ts` and the provider from
`apps/api/src/ai/provider.ts`.

## Security

- **API-key authentication with constant-time comparison.** The presented
  credential is compared against `CERBERUS_API_KEY` using
  `crypto.timingSafeEqual`, without short-circuiting on length. A missing
  credential and a mismatched credential return the same `401 UNAUTHENTICATED`
  body, so the response does not reveal which case occurred. The MCP adapter
  likewise requires `CERBERUS_MCP_TOKEN` and binds to loopback by default.
- **Explicit CORS allow-list.** The `origin: "*"` wildcard was removed.
  Cross-origin access requires an entry in `CERBERUS_CORS_ORIGINS`; with no
  allow-list and no development mode, no cross-origin access is granted.
- **Fail-closed configuration validation.** Mandatory secrets are validated once
  at startup, and the process exits rather than booting into an unauthenticated
  or half-configured state. `CERBERUS_DEV_MODE=true` is refused outright when
  `NODE_ENV=production`. The container entrypoint applies the same check for
  `NODE_ENV=production` with an unset `CERBERUS_API_KEY`.
- **Secrets removed from startup logs.** The configuration banner and the
  service banner report only whether each credential is `set` or `unset`. No key
  material is written to logs or returned in error responses; the global error
  handler returns a stable `INTERNAL_ERROR` code with a correlation id instead of
  framework, provider or database internals.
- **Repository hygiene.** CI scans for verified secrets, rejects committed
  credential-shaped files (`.env`, `application_default_credentials.json`,
  `*.pem`, `*.key`) and fails if a retired deployment or product identifier is
  present.

## Known limitations

These are properties of the release, not defects to be discovered later. Do not
deploy Cerberus expecting them to be absent.

- **Not production ready.** Cerberus is an experimental research system. It has
  no release history, no stability guarantee and no support contract.
- **No guarantee of detection or prevention.** Cerberus makes no claim that it
  will detect, prevent, or prove any particular activity. Treat its output as a
  risk signal for human review, not as evidence or as an authoritative decision.
- **A single shared API key, with no per-user attribution.** There is no user
  directory, no roles and no OAuth/SSO. Cerberus cannot tell two operators apart,
  and revoking one operator's access means rotating the key for everyone.
- **In-memory session state is lost on restart.** Live session state is held in
  process memory. MongoDB is a durable fallback for counters and review data, but
  the authoritative live state does not survive a restart.
- **Reference completions are not populated, so similarity matching returns no
  matches.** `DATA_LEAKAGE_SIMILARITY_THRESHOLD` is read from configuration and
  carried into the analysis prompt, but the reference-completion source returns an
  empty set, so `ExfiltrationReport.matchedSnippets` is always empty.
- **The endpoint agent is not built.** The process that would emit telemetry from
  a real workstation does not exist. All telemetry today originates from the
  browser-based console.
- **No rate limiting.** The API does not throttle requests, and there is no
  replay protection beyond TLS.
- **Manual key rotation only.** There is no automated rotation and no overlap
  window: the API accepts exactly one key at a time, so rotation is a brief
  outage for clients that have not yet been updated.
- **Telemetry can contain sensitive content.** `codeSnapshot`, `pasteSnippets`,
  `copyContent` and `terminalSnapshot` capture whatever was in the monitored
  workspace or clipboard, which may include secrets, customer data or third-party
  material. Monitoring people may require notice, consent or consultation with an
  employee representative body depending on your jurisdiction, and processing
  this data is likely to engage data-protection law. See
  [`SECURITY.md`](../../SECURITY.md) and
  [`docs/security/threat-model.md`](../security/threat-model.md).

## Fixed during release preparation

Two defects were found by exercising the real release candidate rather than the
test suite, and both are fixed in this release.

- **Every AI path was broken against the default model.** The OpenAI provider
  sent a `temperature` on every request — the `OPENAI_TEMPERATURE` default of
  `0.2`, or a hard-coded `0`, `0.1` or `0.2` at individual call sites — and
  `gpt-5.6` rejects any value other than its own default with
  `400 Unsupported value: 'temperature' does not support 0.2 with this model`.
  Scenario authoring, risk analysis, incident recommendations, the
  natural-language auditor and session summarisation all failed closed. The
  parameter is now omitted unless `OPENAI_TEMPERATURE` is explicitly set, so the
  default configuration works and an operator can still opt into a custom value
  on a model that supports one.
- **`scripts/smoke-telemetry.ps1` aborted AI requests it should have waited
  for.** Its scenario step used a 180-second client timeout while the provider
  allows three attempts, so a 5-vector matrix could be cut off mid-flight.
  Raised to 600 seconds. `scripts/verify-all.ps1` also gained
  `-GenerateCount` / `-IngestCount` passthroughs, because it previously always
  ran the burst at its 25-request default and the documented "lower the count"
  instruction was not actually possible.
- **The default AI request timeout was too tight for large scenario matrices.**
  With `OPENAI_REQUEST_TIMEOUT_MS=90000`, a `vectorCount=5` request exceeded the
  per-attempt ceiling on all three attempts and the route returned a retryable
  `AI_UNAVAILABLE`. Measured generation times on the default model were ~11s for
  a classifier verdict, ~40s for a 2-vector matrix and ~89s for a 3-vector
  matrix, so 90s left no headroom. The default is now 180s. This is a tuning
  limit, not a correctness fix: very large matrices (the contract allows up to
  `vectorCount=25`) can still exceed the budget and fail closed with a clear,
  retryable error.

## Verification

This section records the verification state of the release-prep tree. Items left
unchecked are publish blockers: see [`release-checklist.md`](release-checklist.md).
Every ticked line below was executed against this tree during release preparation;
none is carried over from the extraction pass.

- [x] **TypeScript build** — `npm ci` then `npm run build` completed with exit
      code 0 for both workspaces (`@cerberus/api`, `@cerberus/mcp-mongodb`).
- [x] **TypeScript typecheck** — `npm run typecheck` completed with exit code 0
      for both workspaces.
- [x] **Automated tests** — `npm test`: **161 tests across 33 suites, 161 pass,
      0 fail, 0 skipped, 0 todo** (5270 ms). No test calls a paid AI API and none
      needs a live MongoDB; the OpenAI endpoint and the MCP adapter are served by
      an in-process `fetch` stub.
- [x] **Flutter console — dependencies** — `flutter pub get` exits 0.
- [x] **Flutter console — analyze** — `flutter analyze` exits 0 with
      **"No issues found!"**.
- [x] **Flutter console — test** — `flutter test` **2/2 pass** on the Dart VM.
- [x] **Flutter console — formatting** — `dart format --output=none
      --set-exit-if-changed .` exits 0. This initially **failed**: four files
      (`lib/models/scenario_model.dart`, `lib/services/api_service.dart`,
      `lib/widgets/scenario_panel.dart`, `test/widget_test.dart`) were
      unformatted. They were reformatted and the command now reports 0 changed.
      CI does not run this command, so it is a local-only gate.
- [x] **Docker build** — `docker build -t cerberus:v0.1.0 .` completed with exit
      code 0, and again with `--no-cache` (exit 0). Image id `eb8405f621c9`,
      manifest list `sha256:eb8405f621c9222af27c183e839a347017dd91fd8a0df6786a773bc92cf71bf9`.
- [x] **Container startup** — `docker compose up -d --build` brings up `mongo:7`
      (healthy) plus the Cerberus container. `GET /health` returned HTTP 200 with
      `{"status":"healthy","service":"cerberus-api","version":"0.1.0",...}`.
- [x] **MongoDB schema** — verified against the live stack: exactly
      `micro_events`, `monitored_sessions`, `risk_assessments` and
      `threat_scenarios` in the `cerberus` database, each with its intended
      indexes created on connect. `test_suites`, `assessment_sessions` and
      `suspicion_reports` are absent.
- [x] **Fail-closed startup** — verified in three configurations, all exiting 1:
      production with no `CERBERUS_API_KEY`
      (`[entrypoint] FATAL: CERBERUS_API_KEY is not set.`);
      `CERBERUS_DEV_MODE=true` under `NODE_ENV=production`
      ("refused while NODE_ENV=production"); and no `OPENAI_API_KEY`
      (`Missing required environment variable(s): OPENAI_API_KEY`).
- [x] **Authentication boundary** — verified live against a production-shaped
      compose stack (`CERBERUS_DEV_MODE=false`, real shared secrets):
      `GET /health` 200 without a credential; `GET /api/v1/sessions` 401 with no
      credential, 401 with a wrong `Authorization: Bearer`, and 401 with a wrong
      `X-API-Key`; 200 with the correct key on both headers. The MCP adapter
      independently returned 401 without a token, 401 with a wrong token, and 200
      with the correct token, listing its 14 tools.
- [x] **CORS boundary** — verified live with
      `CERBERUS_CORS_ORIGINS=http://allowed.example.test`: a preflight from an
      unlisted origin returned no `Access-Control-Allow-Origin`; a preflight from
      the listed origin echoed it; and the MCP adapter emitted no
      `Access-Control-*` headers at all.
- [x] **Credential-file guard** — no tracked file matches `.env`,
      `application_default_credentials.json`, `*.pem` or `*.key` (excluding
      `.env.example`), and `git ls-files .env` is empty. No build output is
      tracked (`apps/*/dist`, `apps/console/build`, `apps/console/.dart_tool`,
      `*.tsbuildinfo`).
- [x] **Legacy-identifier census** — the CI pattern
      (`webscraping-464710|gorilla_agents|gorilla-mcp-mongodb`) returns no
      matches over the tracked tree. The old product and provider names
      (`FinSec`, `Gemini`) survive only as deliberate provenance text in
      `README.md`, `CHANGELOG.md`, `NOTICE`, `docs/migration.md` and these notes.
- [x] **License inventory** — 106 runtime packages, all permissively licensed
      (MIT 90, ISC 7, Apache-2.0 4, BSD-2-Clause 3, BSD-3-Clause 2), with no
      copyleft, no unknown and no missing license. All seven direct-dependency
      attributions in `NOTICE` were checked against the installed versions and
      all seven are correct.
- [x] **Markdown link audit** — 36 relative path links across the tracked
      Markdown files, all resolving with exact case. One broken in-page anchor in
      `docs/migration.md` was found and fixed.
- [x] **Real OpenAI inference — exercised end to end.** Against a live compose
      stack with a real credential, `POST /api/v1/scenarios` with
      `vectorCount=1` returned **HTTP 201 in 45.3s**: the classifier ran for real
      (`scenarioRelated=true`, `confidence=0.99`, `detectedDomain=financial_services`),
      the matrix parsed with all five top-level keys (`metadata`, `targetSystems`,
      `regulatoryMandates`, `threatVectors`, `penetrationScenarios`),
      `modelVersion` was `gpt-5.6`, token usage was reported
      (46 prompt / 1432 completion), and `persisted` was `true` with one document
      in `threat_scenarios`. A `vectorCount=5` request also completed
      (100s and 129s across two runs, `vectors=5`). No model output is reproduced
      here. Two blocking defects were found and fixed by this exercise — see
      "Fixed during release preparation" above.
- [x] **Live HTTP smoke suites** — run against the real stack with the real
      model and authentication enabled:
      - `scripts/smoke-api.ps1` — **14/14 passed**.
      - `scripts/smoke-telemetry.ps1` — **18/18 passed** (all twelve event types
        plus the deploy/review lifecycle; the large-paste step scored risk 78 with
        4 flags and the session auto-locked).
      - `scripts/stress-telemetry.ps1` at reduced counts
        (`-GenerateCount 1 -IngestCount 5`) — **6/6 requests, 0 failures**;
        43.3s scenario latency, 50ms mean ingest latency.
      - `scripts/verify-all.ps1` at reduced counts — suites 1 and 3 passed; suite
        2 initially failed on two now-fixed client timeouts, and passed 18/18 on
        re-run.
- [x] **No credential leakage.** The live OpenAI key value was checked against
      the full container log stream and against every release-prep log: absent in
      both. The configuration banner reports only `openaiKey=set`. The local key
      file used for this phase was deleted after testing.
- [x] **`trufflehog --only-verified`** — a CI-only job. The binary is not
      installed on the release machine, so the line is confirmed through the
      `Secret scan` CI job rather than a local run: the job is green on the
      release commit, and the repository reports zero secret-scanning alerts.

## Acknowledgements

- The project originated as "Cerberus FinSec" in the Google Cloud Rapid Agent
  Hackathon 2026 (Financial Services track, MongoDB partner track). The hackathon
  setting is where the domain model, the console and the ingestion pipeline were
  first built. The AI boundary moved to the OpenAI SDK afterwards, during OpenAI
  Build Week 2026 (Agentic Coding track).
- Cerberus is built on open-source software, including Hono and
  `@hono/node-server`, the OpenAI Node SDK, the MongoDB Node.js driver, the
  Model Context Protocol SDK, `dotenv`, and the Flutter/Dart SDK packages used by
  the operator console. Direct dependency attributions are listed in
  [`NOTICE`](../../NOTICE).
- Cerberus 0.1.0 has a single author, Muhammad Bilal Raza Lodhi, and there is no
  external contributor history yet. Contributors will be credited here from the
  next release onward.

## License

Apache License 2.0. The full text is in [`LICENSE`](../../LICENSE); third-party
attributions and provenance are in [`NOTICE`](../../NOTICE). The `NOTICE`
copyright line matches the line in `LICENSE`.

Note for the publisher: `NOTICE` covers direct runtime dependencies only, and
says so. A full transitive inventory was generated against this release-prep
tree — **106 packages** in the production closure, all permissively licensed
(MIT 90, ISC 7, Apache-2.0 4, BSD-2-Clause 3, BSD-3-Clause 2), with no copyleft,
no unknown and no missing license. Two transitive packages are Apache-2.0
(`bson`, `mongodb-connection-string-url`) and so carry NOTICE-retention
obligations if their own notices are redistributed; nothing copyleft is present,
so no source-disclosure obligation is triggered. The Flutter console's 33 locked
packages are likewise all permissive. Regenerate the inventory with a tool
against the final release commit rather than relying on this snapshot.
